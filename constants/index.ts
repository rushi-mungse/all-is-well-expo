import { StyleSheet } from "react-native";

export const colors = {
  primary: "#E18026",
  "light-primary": "#E1802670",
};

export const defaultStyle = StyleSheet.create({
  heading: {
    fontSize: 34,
    fontWeight: "700",
    textAlign: "center",
    lineHeight: 38,
  },
});
